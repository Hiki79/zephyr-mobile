<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP is allowed to exactly one place: the core's REST API on loopback,
  which never leaves the device. Everything else, subscriptions included, must
  be HTTPS; Android enforces that at the socket layer, so no code path can
  forget it.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
